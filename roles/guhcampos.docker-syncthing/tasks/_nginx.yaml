---
- name: template nginx config
  ansible.builtin.template:
    src: "docker-syncthing.nginx.subdomain.conf.j2"
    dest: "{{nginx_proxy_config_home}}/docker-syncthing.subdomain.conf"
  notify: restart docker-nginx
  tags: [nginx]
