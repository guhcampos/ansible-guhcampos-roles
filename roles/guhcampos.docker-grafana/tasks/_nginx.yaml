---
- name: template nginx config
  ansible.builtin.template:
    src: "docker-grafana.nginx.subdomain.conf.j2"
    dest: "{{nginx_proxy_config_home}}/docker-grafana.subdomain.conf"
  notify: restart docker-nginx
  tags: [nginx]
