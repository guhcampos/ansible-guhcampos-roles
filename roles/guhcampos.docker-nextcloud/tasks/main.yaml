---

- name: debug
  debug:
    var: hostvars[inventory_hostname]
  tags: debug


- name: "create service group"
  ansible.builtin.group:
    name: "{{nextcloud_unix_group_name}}"
    gid: "{{nextcloud_unix_group_id}}"
    state: present
  register: transmission_group_results

- name: "create service home"
  ansible.builtin.file:
    state: directory
    path: "{{nextcloud_home}}"
    owner: root
    group: adm
    mode: 0755
  notify: "restart docker-nextcloud"

- name: "create user"
  ansible.builtin.user:
    name: "{{nextcloud_unix_user_name}}"
    state: present
    uid: "{{nextcloud_unix_user_id}}"
    group:  "{{nextcloud_unix_group_name}}"
    shell: /usr/sbin/nologin
    home: "{{nextcloud_home}}"
    create_home: false

- name: template docker-compose
  ansible.builtin.template:
    src: "docker-compose.yaml.j2"
    dest: "{{nextcloud_home}}/docker-compose.yaml"
    owner: root
    group: adm
    mode: 0640
  notify: "restart docker-nextcloud"
  tags: [docker-compose]

- name: "create config dir"
  ansible.builtin.file:
    state: directory
    path: "{{nextcloud_config_home}}"
    owner: "{{nextcloud_unix_user_name}}"
    group: "{{nextcloud_unix_group_name}}"
    mode: 0775
  notify: "restart docker-nextcloud"

- name: template systemd service
  ansible.builtin.template:
    src: "docker-nextcloud.service.j2"
    dest: "/lib/systemd/system/docker-nextcloud.service"
  notify: "restart docker-nextcloud"

- name: enable and start docker-nextcloud
  ansible.builtin.systemd_service:
    name: docker-nextcloud
    daemon_reload: true
    enabled: true
    masked: no
    state: started
  async: 60
  poll: 5

- ansible.builtin.include_tasks: "_nginx.yaml"
  tags: [nginx]
