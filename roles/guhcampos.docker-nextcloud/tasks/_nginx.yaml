---
- name: template nginx config
  ansible.builtin.template:
    src: "docker-nextcloud.nginx.subdomain.conf.j2"
    dest: "{{nginx_proxy_config_home}}/docker-nextcloud.subdomain.conf"
  notify: restart docker-nginx
  tags: [nginx]
