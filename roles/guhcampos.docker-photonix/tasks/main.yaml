---
- name: "create service group"
  ansible.builtin.group:
    name: "{{photonix_unix_group_name}}"
    gid: "{{photonix_unix_group_id}}"
    state: present
  register: transmission_group_results

- name: "create service home"
  ansible.builtin.file:
    state: directory
    path: "{{photonix_home}}"
    owner: root
    group: adm
    mode: 0755
  notify: "restart docker-photonix"

- name: "create user"
  ansible.builtin.user:
    name: "{{photonix_unix_user_name}}"
    state: present
    uid: "{{photonix_unix_user_id}}"
    group:  "{{photonix_unix_group_name}}"
    shell: /usr/sbin/nologin
    home: "{{photonix_home}}"
    create_home: false

- name: template docker-compose
  ansible.builtin.template:
    src: "docker-compose.yaml.j2"
    dest: "{{photonix_home}}/docker-compose.yaml"
    owner: root
    group: adm
    mode: 0640
  notify: "restart docker-photonix"
  tags: [docker-compose]

- name: "create config dir"
  ansible.builtin.file:
    state: directory
    path: "{{photonix_config_home}}"
    owner: "{{photonix_unix_user_name}}"
    group: "{{photonix_unix_group_name}}"
    mode: "{{photonix_unix_mode_config_dir}}"
  notify: "restart docker-photonix"

- name: template systemd service
  ansible.builtin.template:
    src: "docker-photonix.service.j2"
    dest: "/lib/systemd/system/docker-photonix.service"
  notify: "restart docker-photonix"

- name: enable and start docker-photonix
  ansible.builtin.systemd_service:
    name: docker-photonix
    daemon_reload: true
    enabled: true
    masked: no
    state: started
  async: 60
  poll: 5

- ansible.builtin.include_tasks: "_nginx.yaml"
  tags: [nginx]
